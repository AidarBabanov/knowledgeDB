<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aidar.knowledgedb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/MyTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--<activity-->
        <!--android:name=".Activities.CompanyActivity"-->
        <!--android:parentActivityName=".Activities.MainActivity">-->
        <!--<meta-data-->
        <!--android:name="android.support.PARENT_ACTIVITY"-->
        <!--android:value=".Activities.MainActivity" />-->
        <!--</activity>-->
        <!--<activity android:name=".Activities.SuperActivity" />-->
        <activity
            android:name=".Activities.IssueActivity"
            android:parentActivityName=".Activities.MainActivity"
            android:windowSoftInputMode="stateAlwaysVisible">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.MainActivity" />
        </activity>
        <activity
            android:name=".Activities.SolveIssueActivity"
            android:parentActivityName=".Activities.IssueActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.IssueActivity" />
        </activity>
        <activity
            android:name=".Activities.AnswerActivity"
            android:label="@string/title_activity_answer"
            android:parentActivityName=".Activities.IssueActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activities.IssueActivity" />
        </activity>
    </application>

</manifest>